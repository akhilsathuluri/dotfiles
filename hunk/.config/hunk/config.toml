mode = "split"             # side-by-side, matching the delta side-by-side setup
theme = "ayu-dark"         # built-in Ayu Dark
line_numbers = true
wrap_lines = true          # wrap long diff lines instead of truncating
